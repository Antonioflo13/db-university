1. Contare quanti iscritti ci sono stati ogni anno (4)

SELECT YEAR(`enrolment_date`)AS `year`, COUNT(*) AS "total_student"
FROM `students`
GROUP BY (`year`);


2. Contare gli insegnanti che hanno l'ufficio nello stesso edificio (29)

SELECT `teachers`.`office_address` , COUNT(*) AS "total_teachers"
FROM `teachers`
GROUP BY `office_address`;

3. Calcolare la media dei voti di ogni appello d'esame (4078)

SELECT COUNT(`exam_id`) AS "total_exams", AVG (`vote`) AS "average"
FROM `exam_student`
GROUP BY `exam_id`;

4. Contare quanti corsi di laurea ci sono per ogni dipartimento (12)

SELECT `departments`.`name`,  COUNT(*) 
FROM `departments`
INNER JOIN `degrees`
ON `degrees`.`department_id`=`departments`.`id`
GROUP BY `degrees`.`department_id`;

